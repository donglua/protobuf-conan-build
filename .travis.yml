os: linux
language: android
dist: xenial
android:
  components:
    - build-tools-27.0.2
    - tools
    - platform-tools
    - extra-google-m2repository
    - extra-android-m2repository
  licenses:
    - 'android-sdk-.+'
    - 'android-sdk-license-.+'
    - 'google-gdk-license-.+'
before_install:
  - sudo apt-get update
  - sudo apt-get -y install ant
install:
  - yes | sudo sdkmanager --licenses
  - echo y | sdkmanager "ndk;16.1.4479499"
  - echo y | sdkmanager "cmake;3.10.2.4988404"
    #  - sdkmanager --update
before_script:
script: echo $ANDROID_SDK
